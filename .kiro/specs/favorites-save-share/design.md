# Technical Design Document

## Introduction

Технический дизайн для реализации функционала избранного, сохранения на доски и шаринга в SnapBoard. Документ описывает архитектуру компонентов, composables и API endpoints.

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        Components Layer                          │
├─────────────────────────────────────────────────────────────────┤
│  FullscreenModal.vue    │  SaveToBoardModal.vue  │  favorites.vue │
│  - Кнопки действий      │  - Список досок        │  - MasonryGrid │
│  - Интеграция модалов   │  - Поиск               │  - Пустое сост.│
│                         │  - Создание доски      │                │
├─────────────────────────────────────────────────────────────────┤
│  image/[id].vue         │  ToastContainer.vue                    │
│  - Страница изображения │  - Глобальные уведомления              │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Composables Layer                          │
├─────────────────────────────────────────────────────────────────┤
│  useFavorites.ts        │  useAuthActions.ts    │  useToast.ts   │
│  - toggleFavorite()     │  - requireAuth()      │  - success()   │
│  - isFavorite()         │  - checkAuth()        │  - error()     │
│  - getFavoriteIds()     │  - redirect logic     │  - info()      │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Store Layer                              │
├─────────────────────────────────────────────────────────────────┤
│  auth.ts                │  boards.ts            │  images.ts     │
│  - isAuthenticated      │  - userBoards         │  - images      │
│  - user                 │  - addImageToBoard()  │  - fetchImages │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                          API Layer                               │
├─────────────────────────────────────────────────────────────────┤
│  /api/boards/[id]/images.post.ts  │  /api/boards/[id]/images.delete.ts │
│  - Добавление изображения         │  - Удаление изображения            │
└─────────────────────────────────────────────────────────────────┘
```

## Component Designs

### Component 1: useAuthActions composable

**Purpose:** Проверка авторизации перед выполнением защищённых действий с редиректом на логин.

**Props/Interface:**
```typescript
interface UseAuthActions {
  requireAuth: (callback: () => void) => void
  checkAuth: () => boolean
}
```

**Behavior:**
- `checkAuth()` - возвращает true если пользователь авторизован
- `requireAuth(callback)` - если авторизован, выполняет callback; иначе редиректит на `/login?redirect={currentPath}`

**Implementation Notes:**
- Использует `useAuthStore` для проверки авторизации
- Использует `useRoute` и `useRouter` для редиректа
- Сохраняет текущий путь в query параметре для возврата после логина

### Component 2: useFavorites composable

**Purpose:** Управление списком избранных изображений с сохранением в localStorage.

**Props/Interface:**
```typescript
interface UseFavorites {
  favoriteIds: Ref<string[]>
  isFavorite: (id: string) => boolean
  toggleFavorite: (id: string) => void
  getFavoriteIds: () => string[]
}
```

**Behavior:**
- Хранит массив ID избранных изображений
- Синхронизирует с localStorage при изменениях
- Загружает данные из localStorage при инициализации

**Implementation Notes:**
- Ключ localStorage: `snapboard_favorites`
- Реактивное состояние через `ref`

### Component 3: SaveToBoardModal.vue

**Purpose:** Модальное окно для выбора доски и сохранения изображения.

**Props:**
```typescript
interface Props {
  isOpen: boolean
  imageId: string | null
  savedBoardIds: string[]
}
```

**Events:**
```typescript
interface Emits {
  close: []
  save: [boardId: string]
  remove: [boardId: string]
}
```

**UI Structure:**
```
┌─────────────────────────────────────┐
│  Сохранить на доску            ✕   │
├─────────────────────────────────────┤
│  🔍 Поиск досок...                  │
├─────────────────────────────────────┤
│  ┌─────┐ Название доски        ✓   │
│  │ img │ N изображений              │
│  └─────┘                            │
├─────────────────────────────────────┤
│  ➕ Создать новую доску             │
└─────────────────────────────────────┘
```

**Implementation Notes:**
- Использует `useBoardsStore` для получения списка досок
- Фильтрация досок по поисковому запросу
- Интеграция с `BoardForm` для создания новой доски

### Component 4: ToastContainer.vue

**Purpose:** Контейнер для отображения toast-уведомлений.

**Implementation Notes:**
- Использует `useToast` composable для получения списка уведомлений
- Позиционирование: fixed, top-right
- Анимация появления/исчезновения через Vue Transition
- Автоматическое удаление через 3 секунды

### Component 5: useToast composable

**Purpose:** Управление toast-уведомлениями.

**Props/Interface:**
```typescript
interface Toast {
  id: string
  type: 'success' | 'error' | 'info'
  message: string
}

interface UseToast {
  toasts: Ref<Toast[]>
  success: (message: string) => void
  error: (message: string) => void
  info: (message: string) => void
  remove: (id: string) => void
}
```

**Implementation Notes:**
- Глобальное состояние через `useState` (Nuxt)
- Автоматическое удаление через `setTimeout`
- Уникальные ID через `Date.now()`

### Component 6: Image Detail Page (/image/[id].vue)

**Purpose:** Страница просмотра отдельного изображения по прямой ссылке.

**Features:**
- Загрузка изображения по ID из URL
- Отображение полной информации: название, описание, теги
- Кнопки действий: избранное, сохранение, шаринг
- Секция похожих изображений
- Обработка ошибки "не найдено"
- SEO: динамический title через `useHead`

**Implementation Notes:**
- Использует `useRoute` для получения ID
- Загружает изображения через `useImagesStore`
- Рекомендации на основе совпадения тегов и слов из названия

## API Endpoints

### POST /api/boards/[id]/images

**Purpose:** Добавление изображения на доску.

**Request Body:**
```typescript
{
  imageId: string
}
```

**Response:**
```typescript
{
  success: boolean
  board: Board
}
```

### DELETE /api/boards/[id]/images

**Purpose:** Удаление изображения с доски.

**Request Body:**
```typescript
{
  imageId: string
}
```

**Response:**
```typescript
{
  success: boolean
}
```

## Data Flow

### Добавление в избранное:
1. Пользователь нажимает ❤️
2. `useAuthActions.requireAuth()` проверяет авторизацию
3. Если не авторизован → редирект на `/login?redirect=...`
4. Если авторизован → `useFavorites.toggleFavorite(id)`
5. Обновление localStorage
6. `useToast.success/info()` показывает уведомление

### Сохранение на доску:
1. Пользователь нажимает 📌
2. `useAuthActions.checkAuth()` проверяет авторизацию
3. Если не авторизован → редирект на логин
4. Если авторизован → открывается `SaveToBoardModal`
5. Пользователь выбирает доску
6. POST `/api/boards/{id}/images`
7. Toast уведомление

### Шаринг:
1. Пользователь нажимает ↗️
2. Формируется URL: `/image/{id}`
3. Попытка `navigator.share()` (Web Share API)
4. Если недоступен → `navigator.clipboard.writeText()`
5. Toast уведомление

## File Structure

```
frontend/
├── components/
│   ├── common/
│   │   └── ToastContainer.vue
│   └── image/
│       ├── FullscreenModal.vue (updated)
│       └── SaveToBoardModal.vue (new)
├── composables/
│   ├── useAuthActions.ts (new)
│   ├── useFavorites.ts (updated)
│   └── useToast.ts (new)
├── pages/
│   ├── favorites.vue (updated)
│   └── image/
│       └── [id].vue (new)
├── server/api/boards/[id]/
│   ├── images.post.ts (new)
│   └── images.delete.ts (new)
└── app.vue (updated - добавлен ToastContainer)
```
