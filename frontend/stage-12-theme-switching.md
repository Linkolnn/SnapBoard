# Этап 12: Тёмная/светлая тема

## 🎯 Цель этапа

Реализовать систему переключения между тёмной и светлой темами с сохранением предпочтений пользователя и поддержкой системной темы.

---

## 📊 Текущее состояние

✅ **ЭТАП ЗАВЕРШЁН**

- ✅ В `variables.sass` определены SASS переменные И CSS переменные для тем
- ✅ CSS переменные используют SASS интерполяцию (`#{$variable}`)
- ✅ Store темы создан и работает (`store/theme.ts`)
- ✅ Composable `useTheme.ts` создан
- ✅ Plugin `theme.client.ts` инициализирует тему
- ✅ ThemeToggle компонент создан и интегрирован
- ✅ Все компоненты мигрированы на CSS переменные

---

## 📖 Глоссарий

- **Theme_Store**: Pinia store для управления состоянием темы (light/dark/system)
- **Theme_Provider**: Composable для доступа к теме и методам переключения
- **Theme_Toggle**: UI компонент для переключения между темами
- **CSS_Variables**: CSS custom properties для динамического изменения цветов
- **System_Theme**: Автоматическое определение темы на основе prefers-color-scheme

---

## 📋 Требования

### Requirement 1: Store темы

**User Story:** Как разработчик, я хочу централизованное управление темой через Pinia store, чтобы состояние темы было доступно во всём приложении.

**Acceptance Criteria:**
1. THE Theme_Store SHALL хранить текущую тему: 'light', 'dark' или 'system'
2. THE Theme_Store SHALL предоставлять computed `resolvedTheme` для получения фактической темы (light/dark)
3. THE Theme_Store SHALL предоставлять метод `setTheme(theme)` для изменения темы
4. THE Theme_Store SHALL предоставлять метод `toggleTheme()` для переключения между light и dark
5. WHEN тема изменяется, THE Theme_Store SHALL сохранять выбор в localStorage
6. WHEN приложение загружается, THE Theme_Store SHALL восстанавливать тему из localStorage

### Requirement 2: CSS переменные для тем

**User Story:** Как разработчик, я хочу использовать CSS переменные для цветов, чтобы тема могла меняться динамически без перезагрузки.

**Acceptance Criteria:**
1. THE CSS_Variables SHALL определять переменные для всех основных цветов
2. THE CSS_Variables SHALL включать: --bg-primary, --bg-secondary, --text-primary, --text-secondary
3. THE CSS_Variables SHALL включать: --border-color, --shadow-color, --accent-color
4. WHEN тема 'light' активна, THE CSS_Variables SHALL использовать светлые значения
5. WHEN тема 'dark' активна, THE CSS_Variables SHALL использовать тёмные значения
6. THE CSS_Variables SHALL применяться через класс на элементе html или body

### Requirement 3: Системная тема

**User Story:** Как пользователь, я хочу чтобы приложение автоматически использовало тему моей системы, чтобы не настраивать её вручную.

**Acceptance Criteria:**
1. WHEN тема установлена в 'system', THE Theme_Provider SHALL определять системную тему через `prefers-color-scheme`
2. WHEN системная тема меняется, THE Theme_Provider SHALL автоматически обновлять тему приложения
3. THE Theme_Provider SHALL использовать `matchMedia` для отслеживания изменений системной темы
4. IF localStorage пуст, THEN THE Theme_Store SHALL использовать 'system' по умолчанию

### Requirement 4: Toggle компонент

**User Story:** Как пользователь, я хочу иметь удобную кнопку для переключения темы, чтобы быстро менять внешний вид приложения.

**Acceptance Criteria:**
1. THE Theme_Toggle SHALL отображать иконку текущей темы (солнце/луна)
2. WHEN пользователь кликает на toggle, THE Theme_Toggle SHALL переключать между light и dark
3. THE Theme_Toggle SHALL иметь плавную анимацию при переключении
4. THE Theme_Toggle SHALL быть доступен в Header для десктопа
5. THE Theme_Toggle SHALL быть доступен в MobileMenu для мобильных устройств
6. THE Theme_Toggle SHALL поддерживать long-press для выбора 'system' (опционально)

### Requirement 5: Миграция компонентов

**User Story:** Как разработчик, я хочу чтобы все компоненты использовали CSS переменные, чтобы тема применялась везде.

**Acceptance Criteria:**
1. THE Components SHALL использовать CSS переменные вместо SASS переменных для цветов
2. THE Components SHALL корректно отображаться в обеих темах
3. THE Components SHALL не иметь жёстко заданных цветов в стилях
4. WHEN тема меняется, THE Components SHALL обновляться без перезагрузки страницы

### Requirement 6: Плавные переходы

**User Story:** Как пользователь, я хочу чтобы смена темы происходила плавно, чтобы не было резких визуальных изменений.

**Acceptance Criteria:**
1. WHEN тема переключается, THE CSS_Variables SHALL применять transition для цветов
2. THE transition SHALL длиться 200-300ms
3. THE transition SHALL применяться к background-color, color, border-color
4. THE transition НЕ SHALL применяться при первой загрузке страницы (избежать flash)

---

## 🏗️ Архитектура

```
Store Layer
└── theme.ts                    - Pinia store для темы

Composables Layer
└── useTheme.ts                 - Composable для работы с темой

Components Layer
├── ThemeToggle.vue             - Кнопка переключения темы
├── Header.vue                  - Интеграция toggle (desktop)
└── MobileMenu.vue              - Интеграция toggle (mobile)

Styles Layer
├── variables.sass              - SASS переменные + CSS переменные для тем
└── reset.sass                  - Импорт variables.sass, применение темы к body

Plugins Layer
└── theme.client.ts             - Инициализация темы на клиенте
```

> **Важно:** CSS переменные для тем находятся в `variables.sass`, а не в отдельном файле.
> Используется SASS интерполяция: `--bg-primary: #{$background-light}`

---

## 🔧 Технический дизайн

### Theme Store (store/theme.ts)

```typescript
interface ThemeState {
  theme: 'light' | 'dark' | 'system'
}

interface ThemeStore {
  theme: 'light' | 'dark' | 'system'
  resolvedTheme: ComputedRef<'light' | 'dark'>
  isDark: ComputedRef<boolean>
  setTheme: (theme: 'light' | 'dark' | 'system') => void
  toggleTheme: () => void
  initTheme: () => void
}
```

### useTheme composable

```typescript
interface UseTheme {
  theme: Ref<'light' | 'dark' | 'system'>
  resolvedTheme: ComputedRef<'light' | 'dark'>
  isDark: ComputedRef<boolean>
  setTheme: (theme: 'light' | 'dark' | 'system') => void
  toggleTheme: () => void
}
```

### CSS переменные (в variables.sass)

```sass
// SASS переменные (базовые значения)
$primary-color: #00dc82
$background-light: #ffffff
$background-dark: #1a1a1a
$text-light: #333333
$text-dark: #ffffff
// ... и другие

// CSS переменные для тем (используют SASS интерполяцию)
\:root, [data-theme="light"]
  --bg-primary: #{$background-light}
  --bg-secondary: #{$gray-100}
  --text-primary: #{$text-light}
  --accent-color: #{$primary-color}
  // ... и другие

[data-theme="dark"]
  --bg-primary: #{$background-dark}
  --bg-secondary: #242424
  --text-primary: #{$text-dark}
  --accent-color: #{$primary-color}
  // ... и другие
```

> **Примечание:** Отдельный файл `themes.sass` НЕ используется.
> Все переменные (SASS и CSS) находятся в `variables.sass`.

### ThemeToggle.vue

```typescript
interface Props {
  size?: 'sm' | 'md' | 'lg'
  showLabel?: boolean
}

// Иконки
// Light: ☀️ или SVG солнце
// Dark: 🌙 или SVG луна
// System: 💻 или SVG монитор
```

---

## 📋 Чеклист реализации

### Фаза 1: CSS переменные и базовая структура

- [x] **1.1** Добавить CSS переменные в `assets/styles/variables.sass`
  - ✅ Определены CSS переменные для светлой темы
  - ✅ Определены CSS переменные для тёмной темы
  - ✅ Используется SASS интерполяция (`#{$variable}`)
  - ✅ Добавлен transition для плавного переключения

- [x] **1.2** Обновить `assets/styles/reset.sass`
  - ✅ Импортирует variables.sass
  - ✅ Применяет CSS переменные к body

### Фаза 2: Store и composable

- [x] **2.1** Создать `store/theme.ts`
  - ✅ State: theme ('light' | 'dark' | 'system')
  - ✅ Getters: resolvedTheme, isDark
  - ✅ Actions: setTheme, toggleTheme, initTheme
  - ✅ Сохранение в localStorage

- [x] **2.2** Создать `composables/useTheme.ts`
  - ✅ Обёртка над store для удобного использования
  - ✅ Отслеживание системной темы через matchMedia

### Фаза 3: Инициализация темы

- [x] **3.1** Создать `plugins/theme.client.ts`
  - ✅ Инициализация темы при загрузке
  - ✅ Применение data-theme к html элементу
  - ✅ Предотвращение flash при загрузке

### Фаза 4: UI компонент

- [x] **4.1** Создать `components/common/ThemeToggle.vue`
  - ✅ Иконка солнца/луны
  - ✅ Анимация при переключении
  - ✅ Поддержка размеров (sm, md, lg)

- [x] **4.2** Интегрировать в Header.vue
  - ✅ Добавлен ThemeToggle в actions секцию
  - ✅ Позиционирование рядом с user menu

- [x] **4.3** Интегрировать в MobileMenu.vue
  - ✅ Добавлен выбор темы (Light/Dark/Auto)
  - ✅ Стилизация для мобильного вида

### Фаза 5: Миграция компонентов

- [x] **5.1** Обновить базовые компоненты
  - ✅ BaseButton.vue - CSS переменные
  - ✅ BaseInput.vue - CSS переменные
  - ✅ BaseIconButton.vue - CSS переменные
  - ✅ BaseTextarea.vue - CSS переменные
  - ✅ BaseTagInput.vue - CSS переменные
  - ✅ BaseModal.vue - CSS переменные
  - ✅ ConfirmModal.vue - CSS переменные

- [x] **5.2** Обновить layout компоненты
  - ✅ Header.vue - фон, текст, границы
  - ✅ Footer.vue - фон, текст (новый компонент)
  - ✅ MobileMenu.vue - фон, текст
  - ✅ default.vue layout - CSS переменные
  - ✅ auth.vue layout - CSS переменные

- [x] **5.3** Обновить карточки и модалы
  - ✅ ImageCard.vue - фон, тени
  - ✅ FullscreenModal.vue - фон, текст
  - ✅ SaveToBoardModal.vue - фон, текст
  - ✅ BoardCard.vue - CSS переменные
  - ✅ BoardForm.vue - CSS переменные

- [x] **5.4** Обновить страницы
  - ✅ index.vue - фон секций
  - ✅ profile.vue - фон, карточки
  - ✅ favorites.vue - фон
  - ✅ boards/index.vue - CSS переменные
  - ✅ boards/[id].vue - CSS переменные

- [x] **5.5** Обновить компоненты поиска
  - ✅ SearchInput.vue - CSS переменные
  - ✅ SearchPanel.vue - CSS переменные
  - ✅ SortSelect.vue - CSS переменные

- [x] **5.6** Обновить компоненты авторизации
  - ✅ LoginForm.vue - CSS переменные
  - ✅ RegisterForm.vue - CSS переменные

### Фаза 6: Тестирование и полировка

- [x] **6.1** Проверить все компоненты в обеих темах
- [x] **6.2** Проверить переходы между темами
- [x] **6.3** Проверить сохранение предпочтений
- [x] **6.4** Проверить системную тему

---

## 🎨 UI Спецификация

### ThemeToggle (Desktop)

```
┌─────┐
│ ☀️  │  <- Светлая тема активна
└─────┘

┌─────┐
│ 🌙  │  <- Тёмная тема активна
└─────┘
```

### ThemeToggle в MobileMenu

```
┌─────────────────────────────────┐
│  Тема                           │
│  ┌─────┐ ┌─────┐ ┌─────┐       │
│  │ ☀️  │ │ 🌙  │ │ 💻  │       │
│  │Light│ │Dark │ │Auto │       │
│  └─────┘ └─────┘ └─────┘       │
└─────────────────────────────────┘
```

### Цветовая схема

**Светлая тема:**
- Фон: #ffffff (primary), #f5f5f5 (secondary)
- Текст: #333333 (primary), #666666 (secondary)
- Границы: #dddddd
- Акцент: #00dc82

**Тёмная тема:**
- Фон: #1a1a1a (primary), #2a2a2a (secondary)
- Текст: #ffffff (primary), #cccccc (secondary)
- Границы: #444444
- Акцент: #00dc82

---

## 🔄 Data Flow

### Инициализация темы:
1. Plugin `theme.client.ts` запускается при загрузке
2. Читает значение из localStorage
3. Если нет - определяет системную тему
4. Применяет `data-theme` к `<html>`
5. Store синхронизируется с DOM

### Переключение темы:
1. Пользователь кликает ThemeToggle
2. `useTheme.toggleTheme()` вызывается
3. Store обновляет state
4. Store сохраняет в localStorage
5. Plugin обновляет `data-theme` на `<html>`
6. CSS переменные автоматически применяются
7. Transition обеспечивает плавный переход

### Системная тема:
1. При theme='system' слушаем `matchMedia('prefers-color-scheme: dark')`
2. При изменении системной темы обновляем resolvedTheme
3. DOM обновляется автоматически

---

## 📁 Файлы для создания

```
frontend/
├── store/
│   └── theme.ts                 # ✅ Pinia store темы
├── composables/
│   └── useTheme.ts              # ✅ Composable для темы
├── plugins/
│   └── theme.client.ts          # ✅ Инициализация темы
└── components/
    ├── common/
    │   └── ThemeToggle.vue      # ✅ Компонент переключения
    └── layout/
        └── Footer.vue           # ✅ Новый компонент футера
```

## 📝 Файлы обновлены

- ✅ `assets/styles/variables.sass` - добавлены CSS переменные для тем
- ✅ `assets/styles/reset.sass` - импорт variables.sass
- ✅ `components/layout/Header.vue` - ThemeToggle + CSS переменные
- ✅ `components/layout/MobileMenu.vue` - выбор темы + CSS переменные
- ✅ `components/common/BaseButton.vue` - CSS переменные
- ✅ `components/common/BaseInput.vue` - CSS переменные
- ✅ `components/common/BaseIconButton.vue` - CSS переменные
- ✅ `components/common/BaseTextarea.vue` - CSS переменные
- ✅ `components/common/BaseTagInput.vue` - CSS переменные
- ✅ `components/common/BaseModal.vue` - CSS переменные
- ✅ `components/common/ConfirmModal.vue` - CSS переменные
- ✅ `components/image/Card.vue` - CSS переменные
- ✅ `components/image/FullscreenModal.vue` - CSS переменные
- ✅ `components/image/SaveToBoardModal.vue` - CSS переменные
- ✅ `components/board/Card.vue` - CSS переменные
- ✅ `components/board/Form.vue` - CSS переменные
- ✅ `components/search/Input.vue` - CSS переменные
- ✅ `components/search/SearchPanel.vue` - CSS переменные
- ✅ `components/search/SortSelect.vue` - CSS переменные
- ✅ `components/auth/LoginForm.vue` - CSS переменные
- ✅ `components/auth/RegisterForm.vue` - CSS переменные
- ✅ `layouts/default.vue` - CSS переменные
- ✅ `layouts/auth.vue` - CSS переменные
- ✅ `pages/index.vue` - CSS переменные
- ✅ `pages/profile.vue` - CSS переменные
- ✅ `pages/favorites.vue` - CSS переменные
- ✅ `pages/boards/index.vue` - CSS переменные
- ✅ `pages/boards/[id].vue` - CSS переменные

## ❌ Файлы удалены

- ❌ `assets/styles/themes.sass` - удалён (CSS переменные перенесены в variables.sass)

---

## ✅ Критерии завершения

1. [x] Store темы работает и сохраняет в localStorage
2. [x] CSS переменные определены для обеих тем (в variables.sass)
3. [x] ThemeToggle отображается в Header и MobileMenu
4. [x] Переключение темы работает плавно
5. [x] Системная тема определяется автоматически
6. [x] Все основные компоненты поддерживают обе темы
7. [x] Нет flash при загрузке страницы
8. [x] Предпочтения сохраняются между сессиями

---

## 📌 Важные заметки

### Структура стилей
- **НЕ используется** отдельный файл `themes.sass`
- Все CSS переменные находятся в `variables.sass`
- Используется SASS интерполяция для переиспользования SASS переменных:
  ```sass
  --bg-primary: #{$background-light}
  ```

### Импорты
- `reset.sass` импортирует `variables.sass`
- Компоненты импортируют `variables.sass` для доступа к SASS переменным
- CSS переменные доступны глобально через `:root` и `[data-theme]`

### Исправления FullscreenModal.vue
- ✅ Фон модала изменён с прозрачного (`--bg-overlay`) на сплошной (`--bg-primary`)
- ✅ Текст в секции actions использует `--text-primary` (белый в тёмной теме, чёрный в светлой)
- ✅ Кнопки навигации и закрытия перемещены внутрь `__image-section`
- ✅ Кнопки имеют фон `--bg-secondary` для лучшей видимости на изображении
- ✅ Исправлена ошибка дублирующихся ключей в v-for для тегов (использован `tag-${index}-${tag}`)
